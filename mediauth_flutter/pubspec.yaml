name: mediauth_flutter
description: "MediAuth AI — Autonomous insurance authorization & appeal platform."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  go_router: ^14.2.7
  intl: ^0.19.0
  file_picker: ^8.1.7
  dotted_border: ^2.1.0
  http: ^1.6.0
  supabase_flutter: ^2.6.0
  google_sign_in: ^6.2.1
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env